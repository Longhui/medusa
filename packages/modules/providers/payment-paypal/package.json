{
  "name": "@medusajs/payment-paypal",
  "version": "2.15.2",
  "description": "PayPal payment provider for Medusa",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/medusajs/medusa",
    "directory": "packages/modules/providers/payment-paypal"
  },
  "files": [
    "dist",
    "!dist/**/__tests__",
    "!dist/**/__mocks__",
    "!dist/**/__fixtures__"
  ],
  "engines": {
    "node": ">=20"
  },
  "author": "Medusa",
  "license": "MIT",
  "scripts": {
    "test": "../../../../node_modules/.bin/jest --passWithNoTests src",
    "build": "yarn run -T rimraf dist && yarn run -T tsc --build ./tsconfig.json",
    "watch": "yarn run -T tsc --watch"
  },
  "devDependencies": {
    "@medusajs/framework": "2.15.2"
  },
  "peerDependencies": {
    "@medusajs/framework": "2.15.2"
  },
  "dependencies": {
    "@paypal/paypal-server-sdk": "^2.3.0"
  },
  "keywords": [
    "medusa-plugin",
    "medusa-plugin-payment"
  ]
}
